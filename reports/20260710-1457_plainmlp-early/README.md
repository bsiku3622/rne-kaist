# An early plain-MLP scanline

One figure, kept because it is the earliest picture of the failure this project spent a
long time on: a smooth network asked to synthesise a sharp peak widens it and flattens it.

Superseded by everything in `archive/`. Kept for the record.

## Sources

Produced by the pre-restructure `model/visualize.py --plane scanline` at 400 W, against a
`PlainNeuralNet` checkpoint that no longer exists.
